#!/usr/bin/env bash
sudo apt-get update && sudo apt-get install -y \
                        libudunits2-dev \
                        default-jdk \
                        libproj-dev \
                        libgdal-dev \
                        cmake \
                        git
                        